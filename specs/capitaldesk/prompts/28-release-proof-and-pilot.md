# Prompt 28 — End-to-end product proof, documentation and pilot package

**Dependencies:** Prompt 27  
**Requirements:** FR-001–FR-024  
**Owns:** release proof, quickstart, demo/video plan and pilot/submission drafts

Read [SESSION-HEADER.md](SESSION-HEADER.md) and the relevant [technical design](../TDD.md), [requirements](../PRD.md) and [test plan](../TEST-PLAN.md) before executing this prompt.

## Copy-paste prompt

```text
Apply the CapitalDesk shared session instructions. Work on module 28 only.

Objective: Hand over an independently usable product with evidenced claims.

Before editing, inspect the actual repository and completed dependency handoffs. State owned paths and missing contracts. Do not infer a dependency is complete because a specification exists.

Implementation tasks:
1. Run a fresh-user complete path using the verified testnet mode: connect, baseline, allocate, two agents, conflict, compatible plan, approval, actual execution, reconciliation and export.
2. Run real response-loss recovery and actual credential-bypass refusal. Link every claim to a tested build, exact source evidence or a labelled deterministic case.
3. Produce architecture/trust diagram, setup docs, operator guide, integration guide, demo shot list and a concise user problem/value narrative.
4. Prepare Track A draft only with currently verified rules and eligibility; preserve pre-event baseline attribution and distinguish existing versus newly built code. No guessed multiple-entry eligibility.
5. Create a bounded operator pilot offer, observed success metrics, discovery interview script and hypothesis-based pricing. Demand remains unproven until real commitments.
6. Write docs/release-readiness.md with PASS/PARTIAL/FAIL, commands, real integrations, limitations and unresolved blockers. Local test success does not waive a failed real execution gate.

Required verification:
T-045–T-054 plus all regression gates; independent exported-ledger calculation and fresh-machine quickstart.
Write the independent failing cases before the consequential implementation. Record actual commands/results; distinguish deterministic tests, real PostgreSQL tests and actual venue evidence.

Acceptance gate:
Required financial and authorization claims are proven; unfinished deployment/pilot/submission is explicitly labelled rather than claimed completed.

Stop condition:
If required source capability, dependency contract, authority or invariant cannot be proven, stop only the affected path and report a concrete blocker. Continue independent in-scope work without replacing the blocked critical path with mocked success.

Handoff:
Update docs/handoffs/28.md using SESSION-HEADER's handoff format. Include exact changed files, tests, real-boundary evidence, limitations and next unblocked modules. Do not mark the full project complete from this module's local tests.
```

