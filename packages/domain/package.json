{
  "name": "@capitaldesk/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure permission model and state reducers. Layer 1: depends only on contracts, and performs no I/O.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty"
  },
  "dependencies": {
    "@capitaldesk/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
