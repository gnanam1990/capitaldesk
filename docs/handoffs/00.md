# Handoff — module 00: repository and capability gate

**Status: PARTIAL.** The repository half is complete; the venue capability half is BLOCKED.

Full evidence: [00-foundation.md](00-foundation.md).

## What this module established

- Repository, toolchain and licence position inspected and recorded. Node 22.23.1,
  pnpm 11.10.0, PostgreSQL 17.10, Docker 29.6.1 available locally. No licence is declared,
  which is an open decision for the repository owner.
- Baseline: `main` holds the reviewed specification pack, verified byte-identical to its
  SHA-256 inventory at import. See the provenance note in
  [00-foundation.md](00-foundation.md) — history was later rewritten, so current baseline
  bytes are the sanitized form.
- Two independent verdicts, as the prompt requires:
  - **Technical execution mode: BLOCKED.** No authenticated Binance credential is configured.
    Account identity, symbol filters, commission rates, IOC behaviour, client-ID constraints,
    rate limits and reset behaviour are all unverified. No probe was run, because running one
    requires a credential that does not exist here.
  - **Competition eligibility: UNVERIFIED.** Not pursued by this build, and not authorized.
- No login, scope grant, order or form submission occurred as a side effect.

## Consequence for later modules

Pure domain work proceeds with typed interfaces. Economic end-to-end work stays blocked. No
write adapter is enabled, and every capability gate in the code refuses by default rather
than assuming availability.

## Blocker

The user must configure an authorized Spot Testnet credential before any capability claim can
be made. Until then, no document in this repository may assert a verified venue capability.
