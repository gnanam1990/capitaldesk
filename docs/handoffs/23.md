# Module 23 — Intent and plan approval UI

**Status: PARTIAL**

The intent queue, opposite-side conflict, target deferral explanation, exact immutable plan review, FIFO participants, fee/debit caps, hypothetical partial-fill explanation, canonical payload digest and distinct expiry clocks are implemented. Owner mutations remain disabled in fixture mode.

## Files

- `apps/web/src/app/intents/page.tsx`
- `apps/web/src/app/plans/page.tsx`
- shared module 21 shell/components/styles and preview model
- `apps/web/src/app/ui-contract.test.ts`

## Behavior

The interface never implies automatic netting. It explains that target deferral survives newer revisions. Plan expiry, `submissionDeadlineAt` and owner-session expiry are separate facts. Native confirmation is a separate post-approval state. Hypothetical FIFO output is explicitly not an actual fill result. Approval and decline are disabled in preview so double-submit or dispatch cannot occur.

## Verification and evidence

The module 21 targeted typecheck, 73 web tests, lint and production build cover both routes. The UI contract test asserts all deadline labels, hypothetical disclosure, preview gating and long-value protections. Browser inspection confirmed the complete plan hierarchy and disabled controls. Actual stale-tab, concurrent owner action and approval persistence need the module 17–20 API/SDK boundary and are not claimed.

## Blockers

Wire generated request/response contracts, CSRF/idempotency handling and fresh-plan replacement once the owner SDK exists. Only then can T-045 keyboard approval and exact executor payload equivalence be proven end to end.
