# Module 24 — Recovery and evidence UI

**Status: PARTIAL**

UNKNOWN recovery, separated transport/venue/accounting state, held liability, partial IOC gross/net/fee allocation, incident timeline, per-asset journal, manifest provenance and verifier semantics are implemented. Live SSE, read-only recovery submission and actual export download wait for their worker/API/SDK contracts.

## Files

- `apps/web/src/app/orders/page.tsx`
- `apps/web/src/app/evidence/page.tsx`
- `apps/web/src/app/settings/page.tsx`
- shared module 21 shell/components/styles
- `apps/web/src/app/ui-contract.test.ts`

## Behavior

UNKNOWN says the exchange may have accepted the order and keeps capital held. The only shown recovery operation is a disabled read-only recheck; no Retry/resend exists. The partial IOC table separates requested gross, actual gross, net ownership, fee asset and unmet target. Export copy states that digest integrity is not a venue signature and preserves unresolved items.

## Verification and evidence

The module 21 targeted typecheck, 73 web tests, lint and production build cover these routes. Static contract tests reject a generic Retry label and require the UNKNOWN explanation and read-only recheck. Browser fixture inspection is not actual venue, SSE, PostgreSQL or export-verifier proof.

## Blockers

Bind to modules 14–20 records, implement SSE reconnect without focus movement, add authenticated halt/resolution requests and verify completed/unresolved exports independently. T-050 remains unproven until a real manifest is generated from durable evidence.
