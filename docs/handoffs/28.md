# Handoff — module 28: release proof and pilot package

**Status: BLOCKED at the real testnet boundary; COMPLETE for release-proof tooling and
operator drafts.** No authenticated venue credential or account was used.

Implemented paths include `tools/proof-manifest.ts`, `tools/proof-cli.ts`, their tests, proof
commands in `package.json`, the architecture, quickstart, operator/integration guides, demo
shot list, pilot package, example descriptor and `docs/release-readiness.md`.

The manifest binds commit, clean-tree state, lockfile, test plan, migration set, mode, stable
account digest, epoch, adapter, fee policy and artifact hashes. Verification fails stale or
sensitive artifacts. The release gate extracts every T-001–T-070 heading and enforces venue,
browser, PostgreSQL, process and mutation proof classes where required.

Local commands and outcomes are recorded in the module commit handoff. Actual T-045, T-053,
response-loss, fresh-account and independently exported venue economics remain blocked. The
pilot and submission material is draft-only; no outreach or publication occurred.

Validation at this branch head:

- `pnpm exec vitest run tools/proof-manifest.test.ts`: 5 passed;
- `pnpm test:unit`: 823 passed;
- `pnpm test:property`: 19 passed;
- `pnpm typecheck`, `pnpm lint` and `pnpm format:check`: passed;
- `pnpm run check:layering`: 13 packages and 115 crossings passed;
- `pnpm run check:secrets`: 337 files passed.
- clean-checkout example manifest creation and verification: passed; the intentionally
  incomplete local bundle was rejected by `release:gate` with the missing scenarios and proof
  classes named.

The final integrated head and actual testnet boundary must repeat these checks and add the
PostgreSQL, browser, process, mutation and venue artifacts required by the release gate.
