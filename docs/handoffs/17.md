# Module 17 handoff — HTTP API and OpenAPI

**Status: PARTIAL**

## Implemented

- Authorized pool state, plan list, exact ledger list, durable event catch-up, reconciliation
  enqueue and evidence-export enqueue routes.
- Cursor and page-size schemas, strict unknown-field refusal, atom/revision strings, correlation
  error envelopes, CSRF on owner/operator mutations and durable job idempotency.
- `/openapi.json` assembled from the same schema constants used by the new runtime routes.
  Documentation examples are explicitly labelled `DOCUMENTATION_EXAMPLES_ONLY`; there is no
  public place-order, signing, dispatch or arbitrary RPC route.

## Files

- `apps/api/src/openapi.ts`, `apps/api/src/openapi.test.ts`
- `apps/api/src/operations/routes.ts`, `apps/api/src/server.ts`
- `packages/db/src/journal/events.ts`, `packages/db/src/journal/events.integration.test.ts`
- `packages/db/migrations/0020_notifications.sql`, `packages/db/src/journal/index.ts`

## Evidence

- Focused API/SDK/worker/agent unit run: 20 tests passed.
- Real PostgreSQL events/jobs: 6 tests passed.
- Existing authenticated API regression: 62 tests passed.
- Package/API/worker builds and test typecheck passed.

## Remaining

The route table is not complete: planner preview/seal, orders, incidents, evidence download and
the module 16 recovery services are absent from this dependency branch. OpenAPI covers the new
operational MVP routes, not every older auth/intent/policy route. No venue call was made.
