# Handoff — module 08: deterministic capital mandates

**Status: PARTIAL.** Owner policy publication, strategy-scoped limits, admission decisions and
durable daily BUY budget holds are implemented. TypeScript and the module-owned unit suite
pass. The new migration and repository tests are present but were not executed against
PostgreSQL in this speed-focused run because no test database URL was available to the task.

## Owned paths

| Path                                       | Purpose                                                                 |
| ------------------------------------------ | ----------------------------------------------------------------------- |
| `packages/domain/src/mandate.ts`           | Strict policy parsing and deterministic admission decisions             |
| `packages/db/migrations/0007_mandates.sql` | Immutable policy versions, strategy limits and BUY budget holds         |
| `packages/db/src/journal/policy.ts`        | Owner publication, active-policy reads and serialized budget accounting |
| `apps/api/src/policies/routes.ts`          | Authenticated owner publish and active-policy HTTP endpoints            |

## Public contract changes

- Every active mandate binds a selected symbol, exact asset identities, per-plan and daily
  limits, a rational concentration bound, all four required freshness classes, plan lifetime,
  buy inhibit, risk halt and fee-policy version.
- Every strategy has its own target, per-plan debit and daily BUY limits. Target acceptance now
  loads that immutable strategy row instead of using the pool maximum as strategy authority.
- BUY budget use equals committed fills plus worst-case outstanding holds. Consumed amounts
  remain in the original UTC bucket; a zero-consumption settlement releases the hold.
- Publication is owner-only, optimistic-concurrency protected and idempotent. It invalidates
  unmarked plans through the existing restoration boundary; marked attempts remain in flight.
- Wire objects reject unknown fields. Monetary inputs remain canonical atom strings and policy
  decisions use bigint or exact rational comparisons.

## Evidence

| Class       | Result                                                          |
| ----------- | --------------------------------------------------------------- |
| typecheck   | pass — workspace TypeScript build and test typings              |
| unit        | pass — 11/11 module mandate cases                               |
| integration | authored — 5 PostgreSQL cases; not run in this task environment |
| real venue  | none — this module makes no venue call                          |

Commands observed:

```sh
pnpm typecheck
pnpm exec vitest run --project unit packages/domain/src/mandate.test.ts
node --env-file-if-exists=.env node_modules/vitest/vitest.mjs run --project integration \
  packages/db/src/journal/policy.integration.test.ts \
  packages/db/src/journal/intents.integration.test.ts
```

The integration command reported `.env not found` and skipped both database suites. This is
recorded as missing execution evidence, not a pass.

## Failure scenarios encoded

- Venue surplus cannot replace an insufficient strategy-owned claim.
- Missing or stale price, account, symbol-metadata or venue-clock evidence fails closed.
- A stale policy/fee version, wrong symbol or asset, expired plan, owner halt, active buy
  inhibit, UTC rollover, daily-budget exhaustion and unprovable concentration are denied with
  stable reason codes.
- Same-pool concurrent holds are serialized under the pool lock so only capacity-backed holds
  can commit.
- Policy rows and strategy limits are immutable; budget holds permit only HELD to CONSUMED or
  RELEASED and are never deleted.

## Limitations and next modules

- PostgreSQL execution of migration `0007` and its repository suite remains a merge/CI gate.
- Module 09 can consume immutable target and policy bounds to create deterministic plans.
- Module 10 can bind policy-budget holds to atomic claim reservations.
- Authenticated venue evidence and funded dispatch remain blocked by missing credentials and
  the unproven real fee bound.
