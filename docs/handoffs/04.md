# Module 04 — Transactional journal, outbox and permanent replay records

**Status: COMPLETE** for the module's owned paths. No repository method performs a network
call, and nothing here has been exercised against a venue.

## What this module establishes

Economic state that is durable and atomically replayable: an append-only ledger balanced per
asset at COMMIT; balances as a projection with one writer; immutable raw observations whose
application is a separate, idempotent act; composite venue identity; one governance lease
per authenticated account across the registry; one open epoch and one in-flight plan per
pool; a forward-only dispatch marker committed with its single-attempt outbox message; worker
leases with fencing tokens; idempotency tombstones that outlive their responses; and a restore
posture that halts, drains nothing and retains every liability.

Design decisions and their limits are in [ADR-0013](../adr/0013-transactional-journal.md).

## Files

| Path                                      | What it is                                                         |
| ----------------------------------------- | ------------------------------------------------------------------ |
| `packages/db/migrations/0003_journal.sql` | Journal schema: tables, partial unique indexes, triggers, rebuild  |
| `packages/db/src/journal/transaction.ts`  | SERIALIZABLE helper with bounded retries and the effect guard      |
| `packages/db/src/journal/governance.ts`   | Venue accounts, governance leases, epoch rotation                  |
| `packages/db/src/journal/ledger.ts`       | Balanced postings, reservations, releases, projection reads        |
| `packages/db/src/journal/dispatch.ts`     | Plans, attempts, the marker, SEND_ATTEMPTED, resolution            |
| `packages/db/src/journal/observations.ts` | Raw observations, apply-once, venue orders and fills               |
| `packages/db/src/journal/outbox.ts`       | Enqueue-with-change, leased claims, acknowledge, fail, dead-letter |
| `packages/db/src/journal/leases.ts`       | Job leases with fencing tokens                                     |
| `packages/db/src/journal/idempotency.ts`  | Begin/record/discard for idempotency tombstones                    |
| `packages/db/src/journal/restore.ts`      | `enterRestorePosture`                                              |
| `packages/db/src/journal/test-harness.ts` | Schema-per-file harness with cancel-first, bounded cleanup         |

## Public contract changes

- `@capitaldesk/db` now exports the journal repositories alongside the migrator.
- Migration `0003_journal` — reserved for module 04 only, as directed. It creates no table
  that another module owns: intents, approvals, policy versions, incidents, reconciliation
  runs and webhook deliveries arrive with the modules that define their behaviour.
- `GovernanceRepository.acquire` is the only way a pool comes into being.

## Evidence

All of it is real PostgreSQL 17.10 on independent connections where concurrency is claimed.
Two cases (the SQL-versus-contract transition table, and the migration scan) need no database
and run inside the same files.

| Count | File                                                        | What it proves                                                                                                                                  |
| ----- | ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| 8     | `packages/db/src/journal/dispatch.integration.test.ts`      | Marker + identity + message commit together; forward-only; one in-flight plan; SQL table equals contract                                        |
| 7     | `packages/db/src/journal/governance.integration.test.ts`    | Two workspaces race one account; rotated credential finds the same governance; liabilities hold the lease; epoch rotation                       |
| 6     | `packages/db/src/journal/ledger.integration.test.ts`        | Explicit baseline; conservation through every transition; two reservers under contention; unbalanced refused at COMMIT; no direct balance write |
| 5     | `packages/db/src/journal/observations.integration.test.ts`  | Applied once across an injected crash; dedupe by source; immutable; composite order/fill identity; late fill                                    |
| 6     | `packages/db/src/journal/outbox-leases.integration.test.ts` | Enqueue with change; leased claim and takeover; dead-letter; dispatch single-attempt; lease tokens and race                                     |
| 5     | `packages/db/src/journal/idempotency.integration.test.ts`   | Replay, conflict, replay-expired tombstone, immutability, record-with-action                                                                    |
| 4     | `packages/db/src/journal/restore.integration.test.ts`       | No migration deletes; rehearsal state survives re-run and restart; restore posture both ways                                                    |
| 5     | `packages/db/src/journal/transaction.integration.test.ts`   | Lock order; SERIALIZABLE; bounded retries; no retry after an effect; real write skew                                                            |

Module total: **46 passed**, 8 files. Workspace integration total at this head is recorded in
`docs/PROJECT-STATUS.md`.

## Failure scenarios proven

Each was confirmed by injecting the defect, observing the named test fail, and restoring.

| Injected defect                                    | Test that failed                                 |
| -------------------------------------------------- | ------------------------------------------------ |
| Deferred balance trigger removed                   | unbalanced posting refused at COMMIT             |
| Pool row lock removed from reservation and posting | two workers reserving under contention           |
| Restore no longer quarantines the outbox           | both restore-posture cases                       |
| Projection write guard disabled                    | no direct write path to balances                 |
| Marker write followed by an injected failure       | no marker and no message (as designed, in-suite) |

One injection did **not** fail a test and was acted on: removing the special retry for a
unique violation in the lease race changed nothing, because PostgreSQL already reports a
concurrent unique violation under SERIALIZABLE as a serialization failure. The path was
removed rather than kept as unproven surface.

## Rehearsal data

`restore.integration.test.ts` builds the state a deployment could be in when upgraded or
restored — a held reservation, a marked attempt at UNKNOWN, a partial fill on a
PARTIALLY_FILLED order, a quarantined HOUSE claim, and an unpublished message — then re-runs
the migrations and reads everything back on a fresh connection. Nothing is cleared, completed
or released.

## Fresh-checkout gate

Run from a clean clone of the branch at `3b3fdbefe8a217fe64354503782230a0467c0292` (the head
carrying both M1 modules and their documentation), with no working-tree state and a frozen
lockfile. The final head of the pull request, which adds only this evidence record and one
harness teardown fix, is named in the pull request body rather than here, so this record does
not have to be rewritten by the commit that would change it.

```sh
git clone --no-local <repo> fresh && cd fresh && git checkout 3b3fdbefe8a217fe64354503782230a0467c0292
pnpm install --frozen-lockfile
pnpm run format:check
pnpm run typecheck
pnpm run lint
pnpm run check:layering
pnpm run check:secrets
pnpm run test:unit
pnpm run test:property
CAPITALDESK_TEST_DATABASE_URL=postgres://localhost:5432/capitaldesk_test pnpm run test:integration
pnpm run build
```

| Command                          | Result                                     |
| -------------------------------- | ------------------------------------------ |
| `pnpm install --frozen-lockfile` | pass                                       |
| `pnpm run format:check`          | pass                                       |
| `pnpm run typecheck`             | pass                                       |
| `pnpm run lint`                  | pass                                       |
| `pnpm run check:layering`        | pass — 9 packages, 45 crossings checked    |
| `pnpm run check:secrets`         | pass — 232 files scanned                   |
| `pnpm run test:unit`             | **507 passed**, 31 files                   |
| `pnpm run test:property`         | **13 passed**, seed 20260908               |
| `pnpm run test:integration`      | **181 passed**, 17 files, PostgreSQL 17.10 |
| `pnpm run build`                 | pass — every package and app               |

Toolchain: Node 22.23.1, pnpm 11.10.0, PostgreSQL 17.10 (Homebrew, local). No database
sessions remained after the run.

## Limitations

- **No role-based REVOKE.** Append-only and immutable tables are enforced by triggers, which
  bind every role including the schema owner the tests run as. Revoking `UPDATE`/`DELETE`
  from an application role is a deployment control, not applied here.
- **The projection guard is a session flag.** A session that sets it deliberately can write
  `claim_balances`; the same session could rewrite the entries. It is a guard against
  application mistakes, not against a hostile database connection.
- **Lease expiry fences the database only.** A sender holding a marker can complete its send
  after its lease lapses; nothing here, or anywhere, can fence the venue.
- **Deferred tables.** Intents, approvals, policy versions, incidents, reconciliation runs and
  webhook deliveries are not created by this migration.
- **Independent deployments** that do not share this registry cannot enforce the single
  governance lease. That is a documented trust limit, not a defect this code can close.

## Next

Modules 05 (account reader), 06 (baseline ledger) and 07 (intents) depend on this journal
and are unblocked. The M1 pull request is opened after the fresh-checkout gate.
