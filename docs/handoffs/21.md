# Module 21 — UI foundation

**Status: PARTIAL**

The responsive application shell, semantic visual system, navigation, persistent account/environment context, command palette, accessible state components, exact quantity presentation and isolated state gallery are implemented. Live typed-SDK data binding and authenticated owner actions remain blocked because module 19 is not present on this dependency branch.

## Files

- `apps/web/src/app/layout.tsx`
- `apps/web/src/app/globals.css`
- `apps/web/src/app/ui-states/page.tsx`
- `apps/web/src/components/AppShell.tsx`
- `apps/web/src/components/CommandPalette.tsx`
- `apps/web/src/components/Console.tsx`
- `apps/web/src/components/Icons.tsx`
- `apps/web/src/components/StatusPill.tsx`
- `apps/web/src/lib/preview-data.ts`
- `apps/web/src/lib/preview-data.test.ts`
- `apps/web/src/app/ui-contract.test.ts`
- `apps/web/.env.example`

## Public contract

`CAPITALDESK_UI_PREVIEW=true` enables labelled, development-only interface fixtures at build time. The default is false. Without the switch, data-dependent routes render an honest SDK-unavailable state. No credential or secret reference reaches the browser.

## Verification

- `pnpm exec tsc -b && pnpm --filter @capitaldesk/web typecheck` — pass.
- `pnpm exec vitest run --project unit apps/web/src` — pass, 73 tests after the final UI contract additions.
- `pnpm exec eslint apps/web/src` — pass.
- `pnpm --filter @capitaldesk/web build` — pass; eleven routes generated with preview mode disabled.
- Production-mode HTTP probe of `/capital` — rendered the SDK-unavailable boundary and contained neither the fixture disclosure nor its sample balance.
- Browser inspection at desktop width — shell, active navigation, long quantities, fixture disclosure and plan hierarchy rendered; pointer and `⌘K` both opened the command palette and focused its labelled search field. Automated 375/768/1024/1440 capture is not claimed.

## Proven failure states

SDK absent, API unreachable, pending evidence, stale source, empty baseline, denied owner scope, unsupported execution, reset epoch, UNKNOWN and long identifiers/amounts are visually explicit. Fixture controls cannot approve, dispatch, recheck or publish.

## Real boundary

No venue account was used. Browser evidence is labelled fixture-only. Module 22–24 presentation work is ready; live end-to-end acceptance waits for modules 17–20 and real worker records.
