# Requirements traceability

Every functional requirement and core invariant, mapped to the code that implements it and
the tests that prove it, with an honest proof class.

Read the **Proof** column carefully. `unit` and `property` are deterministic reference models
over pure functions. `integration` is a real PostgreSQL server and the real HTTP server.
`browser` is Playwright against the running console. **No row anywhere in this document is
backed by venue evidence, because no Binance account has been contacted.**

Most rows below are `not started`. This is the foundation milestone; the economic core is
M1-M3. A requirement listed as `not started` has no partial implementation hiding behind it.

## Proof classes

| Class         | What it means                                                                                   |
| ------------- | ----------------------------------------------------------------------------------------------- |
| `unit`        | Deterministic test over pure functions                                                          |
| `property`    | Generated cases at a recorded seed, with an independent oracle where the property is arithmetic |
| `integration` | Real PostgreSQL, real HTTP server, real processes                                               |
| `browser`     | Playwright against the running application                                                      |
| `venue`       | Authenticated exchange interaction — **none exists**                                            |
| `gate-only`   | The decision gate is implemented and tested; the producer of the evidence it consumes is not    |

## Functional requirements

| ID     | Requirement                                     | Implementation                                                                                                | Tests                                                                               | Proof             | Status                                                                                                                                                                       |
| ------ | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FR-001 | Identify account and capability mode explicitly | `packages/contracts/src/identity.ts`, `packages/config/src/env.ts`, `apps/api/src/health.ts`                  | `identity` via digest tests, `env.test.ts`, `health.test.ts`                        | unit, integration | partial — identity types and truthful capability reporting exist; no venue identity is established                                                                           |
| FR-002 | Establish a reconciled baseline                 | —                                                                                                             | —                                                                                   | —                 | not started (module 06)                                                                                                                                                      |
| FR-003 | Isolate agent authority                         | `packages/domain/src/capabilities.ts`, `principal.ts`, `apps/api/src/auth/`, `tools/check-secret-boundary.ts` | `principal.test.ts` (29), `auth.integration.test.ts` (49)                           | unit, integration | partial — an agent is bound to one strategy and holds only `intent.propose` and reads; process isolation is module 12                                                        |
| FR-004 | Maintain virtual strategy claims                | `packages/contracts/src/money.ts`                                                                             | `money.test.ts`, `money.property.test.ts`                                           | unit, property    | partial — exact per-asset quantities frozen; the ledger is module 06                                                                                                         |
| FR-005 | Enforce budgets separately from venue balance   | `packages/contracts/src/risk.ts`, `marked-value.ts`                                                           | `risk.test.ts`                                                                      | unit, property    | partial — marked value structurally cannot fund a reservation; budget engine is module 08                                                                                    |
| FR-006 | Accept versioned absolute targets               | `packages/contracts/src/states.ts` (intent transitions)                                                       | `states` transition tests                                                           | unit              | partial — states frozen; intent service is module 07                                                                                                                         |
| FR-007 | Account for existing commitments                | `packages/contracts/src/states.ts`                                                                            | —                                                                                   | —                 | not started (module 07)                                                                                                                                                      |
| FR-008 | Surface opposite-direction conflict             | `packages/contracts/src/reason-codes.ts`                                                                      | —                                                                                   | —                 | not started (module 09)                                                                                                                                                      |
| FR-009 | Apply explicit risk mandates                    | `packages/contracts/src/risk.ts`                                                                              | `risk.test.ts`                                                                      | unit, property    | partial — concentration arithmetic and fail-closed behaviour frozen; the mandate engine is module 08                                                                         |
| FR-010 | Reserve capital atomically                      | `packages/db/src/journal/ledger.ts` (`reserve`), `0003_journal.sql`                                           | `ledger.integration.test.ts` (two connections, barrier)                             | integration       | partial — SERIALIZABLE reservation against the strategy's AVAILABLE claim, proven under contention; venue free-capacity check is module 10                                   |
| FR-011 | Construct a real compatible plan                | —                                                                                                             | —                                                                                   | —                 | not started (module 09)                                                                                                                                                      |
| FR-012 | Freeze allocation before approval               | `packages/contracts/src/plan-digest.ts`                                                                       | `plan-digest.test.ts`                                                               | unit              | partial — FIFO schedule bound into the digest; the planner is module 09                                                                                                      |
| FR-013 | Bind approval to exact execution                | `packages/contracts/src/plan-digest.ts`, `canonical.ts`                                                       | `plan-digest.test.ts`, `canonical.test.ts`                                          | unit              | implemented at contract level — all 20 bound fields, unknown-field rejection, sparse-array rejection                                                                         |
| FR-014 | Persist before dispatch                         | `packages/db/src/journal/dispatch.ts`, `0003_journal.sql` (forward-only trigger, single-attempt outbox)       | `dispatch.integration.test.ts`, `states` tests                                      | integration, unit | partial — marker, identity and send message commit together; no resend path exists; the executor is module 13                                                                |
| FR-015 | Preserve execution ambiguity                    | `packages/contracts/src/states.ts`, `dispatch-envelope.ts`                                                    | `dispatch-envelope.test.ts`                                                         | unit              | partial — UNKNOWN, NOT_SENT_PROVEN and IRRECOVERABLE_UNCERTAINTY frozen; recovery is module 15                                                                               |
| FR-016 | Allocate only actual fills                      | `packages/ledger/src/fill-allocation.ts`, `packages/db/src/journal/reconciliation.ts`                         | `fill-allocation.test.ts`, `reconciliation.integration.test.ts`                     | unit, integration | partial — actual fill quantity and fees allocate in exact atoms with deterministic FIFO controlled rounding and immutable postings; a real venue fill bundle is still absent |
| FR-017 | Complete partial and terminal reconciliation    | `packages/reconciler/src/finality.ts`, `packages/db/src/journal/reconciliation.ts`                            | `finality.test.ts`, `reconciliation.integration.test.ts`                            | unit, integration | partial — incomplete coverage and unresolved status retain capital; authenticated live pagination and an account cut remain unproven                                         |
| FR-018 | Detect and quarantine drift                     | `packages/reconciler/src/drift.ts`, `packages/db/src/journal/recovery.ts`                                     | `drift.test.ts`, `recovery.integration.test.ts`, `observations.integration.test.ts` | unit, integration | partial — unexplained balances/orders and conflicting observations halt and quarantine; no live external-movement drill exists                                               |
| FR-019 | Recover through evidence                        | `packages/reconciler/src/recovery.ts`, `packages/db/src/journal/recovery.ts`                                  | `recovery.test.ts`, `recovery.integration.test.ts`                                  | unit, integration | partial — evidence-bound reopen and NOT_SENT_PROVEN paths exist; credentialed restart and response-loss proof is still missing                                               |
| FR-020 | Expose a modern operational console             | `apps/web`                                                                                                    | `tokens.test.ts`, browser verification                                              | unit, browser     | partial — shell, tokens, one truthful page; the owner workflow is modules 22-24                                                                                              |
| FR-021 | Provide auditable integration surfaces          | `apps/api/src/server.ts`, `apps/api/src/auth/routes.ts`, `apps/api/src/main.ts`                               | `auth.integration.test.ts` (49), `main.integration.test.ts` (3)                     | integration       | partial — identity routes and an audited surface exist and are served by the shipped process; the economic `/v1` surface is module 17                                        |
| FR-022 | Preserve event provenance                       | `packages/db/src/journal/observations.ts`, `ledger.ts`, `0003_journal.sql`                                    | `observations.integration.test.ts`, `ledger.integration.test.ts`                    | integration       | partial — immutable observations, append-only balanced ledger, composite venue identity; producers of observations are modules 05 and 15                                     |
| FR-023 | Exercise actual boundary failures               | `packages/db/src/migrator.integration.test.ts`                                                                | 22 integration cases                                                                | integration       | partial — database boundaries only; the fault lab is module 25                                                                                                               |
| FR-024 | Ship an operable installation                   | `package.json`, `.github/workflows/ci.yml`, `packages/db`, `apps/api/src/main.ts`                             | CI, migration and restore integration tests, `main.integration.test.ts`             | integration       | partial — install, migrate, restart and restore posture proven; the full journey does not exist                                                                              |

| FR-025 | Authenticate the owner without a default credential | `apps/api/src/cli/enroll.ts`, `args.ts`, `input-session.ts`, `apps/api/src/auth/routes.ts` | `enroll.integration.test.ts` (8), `enroll-cli.integration.test.ts` (10), `args.test.ts` (6), `input-session.test.ts` (4) | unit, integration | implemented — one-time enrollment, single-use under concurrency, no default owner or shared secret |
| FR-026 | Scope every lookup by workspace, pool and strategy | `packages/db/migrations/0002_identity.sql`, `apps/api/src/auth/repository.ts` | `identity-scope.integration.test.ts` (11), `auth.integration.test.ts` (49) | integration | implemented — composite foreign keys refuse a real identifier presented under the wrong scope |

## Invariants

| ID     | Invariant                                       | Implementation                                                                                    | Tests                                                                   | Proof                       | Status                                                                                                                |
| ------ | ----------------------------------------------- | ------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | --------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| INV-01 | An agent can never dispatch or sign             | `capabilities.ts` (`NEVER_GRANTED`), `principal.ts`, `tools/`                                     | `principal.test.ts`, `check-layering.test.ts`                           | unit, static                | partial — `venue.dispatch` is named and denied to every role; no dispatch path exists yet                             |
| INV-02 | Ledger balances per asset; assets never mixed   | `money.ts`, `0003_journal.sql` (deferred balance trigger), `ledger.ts`                            | `money.test.ts`, `money.property.test.ts`, `ledger.integration.test.ts` | unit, property, integration | implemented — an unbalanced posting is refused before write and again at COMMIT                                       |
| INV-03 | Claims are nonnegative                          | `money.ts` (`subtractAmounts`), `0003_journal.sql` (`claim_balances_nonnegative`)                 | `money.test.ts`, property, `ledger.integration.test.ts`                 | unit, property, integration | implemented — the projection refuses a negative claim rather than storing one                                         |
| INV-04 | Control balance equals claims per asset         | `0003_journal.sql` (deferred balance trigger), `ledger.ts`                                        | `ledger.integration.test.ts` (conservation through every transition)    | integration                 | implemented at the ledger; the bootstrap producer is module 06                                                        |
| INV-05 | Reservations fit claims and reconciled capacity | `risk.ts`                                                                                         | `risk.test.ts`                                                          | unit                        | partial                                                                                                               |
| INV-06 | One immutable preapproved allocation list       | `plan-digest.ts`                                                                                  | `plan-digest.test.ts`                                                   | unit                        | implemented at contract level                                                                                         |
| INV-07 | Allocated quantity and commission sum exactly   | `packages/ledger/src/fill-allocation.ts`, `packages/db/src/journal/reconciliation.ts`             | `fill-allocation.test.ts`, `reconciliation.integration.test.ts`         | unit, integration           | implemented for persisted fills — quantity, quote debit and each fee asset conserve exactly under controlled rounding |
| INV-08 | Approval binds the exact payload                | `plan-digest.ts`, `canonical.ts`                                                                  | `plan-digest.test.ts` (per-field), `canonical.test.ts`                  | unit                        | implemented at contract level                                                                                         |
| INV-09 | Marker before send; no automatic re-dispatch    | `states.ts` (`mayAutomaticallyResend`), `dispatch-envelope.ts`, `dispatch.ts`, `0003_journal.sql` | `states`, `dispatch-envelope.test.ts`, `dispatch.integration.test.ts`   | unit, integration           | partial — marker commits with its single-attempt message; a marked attempt cannot regress; the executor is module 13  |
| INV-10 | Uncertainty cannot release funds                | `states.ts` (`mayReleaseUnusedReservation`), `observation.ts`                                     | `states`, `observation.test.ts`                                         | unit                        | implemented at contract level                                                                                         |
| INV-11 | Idempotent replay                               | `lifecycle.ts` (idempotency scopes), `idempotency.ts`, `0003_journal.sql` (tombstone triggers)    | `lifecycle.test.ts`, `idempotency.integration.test.ts`                  | unit, integration           | implemented — same body replays, changed body conflicts, expired response never re-runs the action                    |
| INV-12 | No strategy consumes another's claim            | `money.ts`, `risk.ts`                                                                             | `money.test.ts`, `risk.test.ts`                                         | unit, property              | partial                                                                                                               |
| INV-13 | Opposing intents are explicit conflicts         | `reason-codes.ts`, ADR-0008                                                                       | —                                                                       | —                           | not started (module 09)                                                                                               |
| INV-14 | Environments and epochs never share identity    | `identity.ts` (`requireSamePool`), `config/env.ts`                                                | `plan-digest.test.ts`, `env.test.ts`                                    | unit                        | implemented at contract level                                                                                         |
| INV-15 | Terminal status is not complete accounting      | `states.ts`                                                                                       | `states` tests                                                          | unit                        | implemented at contract level                                                                                         |
| INV-16 | Projections rebuild from immutable observations | `0003_journal.sql` (`rebuild_claim_balances`, write guard), `ledger.ts`                           | `ledger.integration.test.ts` (rebuild equals independent computation)   | integration                 | implemented — the projection has one writer, the rebuild, and is refreshed before it is read                          |

## Amendment scenarios

| ID    | Scenario                                                 | Tests                                                                                                                  | Proof                       | Status                                                                                                                                                                                  |
| ----- | -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-006 | Bootstrap requires an explicit one-time code             | `enroll.integration.test.ts`, `enroll-cli.integration.test.ts`                                                         | integration                 | implemented — reissue after expiry, rotation, single use under two concurrent redeemers                                                                                                 |
| T-036 | An agent cannot act outside its bound scope              | `principal.test.ts`, `auth.integration.test.ts`                                                                        | unit, integration           | implemented                                                                                                                                                                             |
| T-037 | A caller-controlled field never selects authority        | `auth.integration.test.ts`                                                                                             | integration                 | implemented — principals are built only from stored columns                                                                                                                             |
| T-039 | CSRF and token-class confusion are refused               | `auth.integration.test.ts`                                                                                             | integration                 | implemented — an agent bearer cannot reach the session lifecycle                                                                                                                        |
| T-040 | A one-time secret cannot be replayed                     | `auth.integration.test.ts`, `enroll.integration.test.ts`                                                               | integration                 | implemented — no route returns a secret again; a lost issuance response is recovered by naming the live key and rotating it                                                             |
| T-044 | Secrets never reach logs, exports or audit records       | `audit-detail.test.ts`, `auth.integration.test.ts`, `enroll-cli.integration.test.ts`                                   | unit, integration           | implemented for this module's sinks — `detail` is a closed schema; actor and scope columns derive from server-established values, never a route parameter; logs pass the redacting sink |
| T-011 | Opening baseline ownership is explicit                   | `packages/db/src/journal/baseline.integration.test.ts`                                                                 | integration                 | implemented (module 06) — every opening unit is a HOUSE claim, no strategy is credited by symbol match, excluded assets and unknown cost basis recorded                                 |
| T-012 | Per-asset conservation through every transition          | `packages/ledger/src/conservation.ts`, `baseline.integration.test.ts`, `ledger.integration.test.ts`                    | unit, property, integration | partial — conservation recomputed independently for opening, allocation, reserve and release; fills, commissions and quarantine arrive with module 14                                   |
| T-013 | Account quota contention in PostgreSQL                   | `baseline.integration.test.ts`, `ledger.integration.test.ts` (independent connections, barrier)                        | integration                 | implemented — two allocators and two reservers race the same balance on independent backends; exactly one commits                                                                       |
| T-024 | Durable marker precedes all sends                        | `dispatch.integration.test.ts`                                                                                         | integration                 | partial — marker, identity and send message commit together and cannot regress; the interception of network paths is module 13                                                          |
| T-030 | Crash during allocation commit                           | `observations.integration.test.ts`, `ledger.integration.test.ts`                                                       | integration                 | implemented at the journal — an observation applied once across an injected crash; unique source operation                                                                              |
| T-031 | Order identity includes account, epoch and symbol        | `observations.integration.test.ts`                                                                                     | integration                 | implemented — venue ids reused across symbol and epoch are distinct rows; same-scope reuse refused                                                                                      |
| T-032 | Testnet reset invalidates the epoch                      | `governance.integration.test.ts`, `venue-read.integration.test.ts`, `baseline.integration.test.ts`, `recovery.test.ts` | unit, integration           | partial — rotation and reset evidence halt the affected pool; a credentialed reset drill remains missing                                                                                |
| T-033 | Cancel/expiry races with late fills                      | `observations.integration.test.ts`, `finality.test.ts`, `reconciliation.integration.test.ts`                           | unit, integration           | implemented at the journal/reconciler boundary — late fills remain ingestible and terminal status alone never releases reserved capital                                                 |
| T-034 | Pagination proves coverage                               | `reader.test.ts`, `ingest.integration.test.ts`, `observations.integration.test.ts`, `finality.test.ts`                 | unit, integration           | partial — duplicate page boundaries deduplicate and incomplete coverage holds capital; authenticated multi-page venue proof is absent                                                   |
| T-035 | Restore starts in reconciliation mode                    | `restore.integration.test.ts`                                                                                          | integration                 | implemented — pools halted, outbox quarantined, unmarked plans invalidated, liabilities retained                                                                                        |
| T-051 | Migration and restart preserve unresolved liabilities    | `restore.integration.test.ts` (rehearsal fixture, migration scan)                                                      | integration                 | implemented — reserved, UNKNOWN, partial and quarantined state survive a re-run and a fresh connection; no migration deletes                                                            |
| T-056 | One stable account cannot bootstrap twice                | `governance.integration.test.ts`, `baseline.integration.test.ts`                                                       | integration                 | implemented — one lease across workspaces, rotated credential finds the same governance, liabilities hold the lease                                                                     |
| T-014 | Golden partial fill and expiry                           | `fill-allocation.test.ts`, `finality.test.ts`, `reconciliation.integration.test.ts`                                    | unit, integration           | implemented at deterministic and PostgreSQL boundaries — exact allocation persists once and incomplete reconciliation retains the remainder                                             |
| T-020 | Strategy ownership stays separate                        | `packages/ledger/src/allocation.ts`, `baseline.integration.test.ts`                                                    | unit, integration           | partial (module 06) — a strategy-to-strategy move is refused by the service and by the table; market disposal accounting is module 14                                                   |
| T-023 | Property-based money-state exploration                   | `packages/ledger/src/conservation.property.test.ts`, `money.property.test.ts`                                          | property                    | partial — conservation, non-negativity and allocation sequences at seed 20260908; fills, fee assets and approval invalidation once those transitions exist                              |
| T-019 | Decimal boundaries and exchange filters                  | `packages/binance/src/decode.test.ts`                                                                                  | unit                        | partial (module 05) — exact scaled atoms, filter bounds, zero-disabled price parts and precision refusals; order sizing is module 09                                                    |
| T-021 | Reservation lifetime requires complete evidence          | `reader.test.ts`, `ingest.integration.test.ts`, `finality.test.ts`, `reconciliation.integration.test.ts`               | unit, integration           | partial — the release gate requires terminal status, complete coverage and settled accounting; authenticated venue coverage is absent                                                   |
| T-022 | Duplicate, reordered and corrected provider records      | `reader.test.ts`, `observations.integration.test.ts`, `reconciliation.integration.test.ts`                             | unit, integration           | implemented at decode/journal boundaries — duplicates have one effect, and same-identity conflicts quarantine instead of overwriting                                                    |
| T-029 | A single NOT_FOUND cannot release or retry               | `packages/binance/src/reader.test.ts`                                                                                  | unit                        | implemented at the read boundary — −2013 is a typed rejection carrying its code, never an absence; a later query reveals the order                                                      |
| T-041 | External orders and balance drift                        | `apps/worker/src/ingest.integration.test.ts` (account-wide scan)                                                       | integration                 | implemented at the read boundary — an unknown resting order on any symbol is detected and reported unexplained rather than attributed                                                   |
| T-042 | Baseline exclusions remain visible                       | `venue-read.integration.test.ts`, `baseline.integration.test.ts`                                                       | integration                 | partial (module 05) — a cut carries its verdict, every unmet reason and the enumerated symbol set; exports and UI are module 24                                                         |
| T-043 | Rate limits, degraded reads and authentication expiry    | `failures.test.ts`, `transport.test.ts`, `reader.test.ts`                                                              | unit                        | implemented at the read boundary — 429/418 with Retry-After become a persisted absolute defer; 5xx and auth failures are typed, never an empty list                                     |
| T-038 | Broker-key and approved-host modes never mix             | `packages/binance/src/transport.test.ts`                                                                               | unit                        | partial (module 05) — VENUE_TRADE cannot construct a reader and no signed read reaches an unapproved origin; the executor half is module 12                                             |
| T-059 | Fenced sender proves the order was never sent            | `finality.test.ts`, `recovery.integration.test.ts`                                                                     | unit, integration           | partial — a stored positive fence can prove NOT_SENT_PROVEN without releasing unresolved liabilities; no real transmitter-side fence artifact exists                                    |
| T-060 | Without a fence, absence is never proven                 | `finality.test.ts`, `recovery.integration.test.ts`                                                                     | unit, integration           | partial — the decision contract refuses unfenced absence and the database path requires positive fencing; no credentialed response-loss drill exists                                    |
| T-061 | Coverage predicate, condition by condition               | `observation.test.ts`                                                                                                  | unit, gate-only             | implemented                                                                                                                                                                             |
| T-062 | Agreeing balances alone do not establish coverage        | `observation.test.ts`                                                                                                  | unit                        | implemented                                                                                                                                                                             |
| T-063 | A paused transmitter cannot be accepted late             | `dispatch-envelope.test.ts`                                                                                            | unit                        | implemented — checked against an independent model of the venue predicate                                                                                                               |
| T-064 | An unknown venue status quarantines                      | `states.ts`, `packages/reconciler/src/finality.ts`, `packages/db/src/journal/recovery.ts`                              | unit, integration           | implemented at the reconciliation boundary — unrecognized or conflicting state cannot finalize and drives quarantine                                                                    |
| T-065 | Recovery from the authorization evidence bundle          | —                                                                                                                      | —                           | not started (module 27)                                                                                                                                                                 |
| T-066 | Lifecycle effects at marked and unmarked states          | `lifecycle.test.ts`                                                                                                    | unit                        | implemented                                                                                                                                                                             |
| T-067 | Deferral survives a newer revision                       | `lifecycle.test.ts`                                                                                                    | unit                        | implemented at contract level                                                                                                                                                           |
| T-068 | Late opposing intent before and after the marker         | —                                                                                                                      | —                           | not started (module 09)                                                                                                                                                                 |
| T-069 | Concentration uncomputable and reassignment-invariant    | `risk.test.ts`, `money.property.test.ts`                                                                               | unit, property              | implemented                                                                                                                                                                             |
| T-070 | An unproven fee policy refuses dispatch                  | `fee-policy.test.ts`                                                                                                   | unit, gate-only             | implemented                                                                                                                                                                             |
| T-071 | Migration status never writes; divergent history refuses | `migrator.integration.test.ts`                                                                                         | integration                 | implemented                                                                                                                                                                             |

### Coverage with no TEST-PLAN scenario id

Module 03's session-lifecycle evidence was previously filed as `T-038`, which is TEST-PLAN's
"Broker-key and approved-host modes never mix" — a different scenario entirely. The evidence is
real and is kept; only the identifier was wrong, and no new id is invented for it here.

| Behaviour                                                     | Evidence                   | Class       | Status                                                    |
| ------------------------------------------------------------- | -------------------------- | ----------- | --------------------------------------------------------- |
| Expired, idle, revoked and rotated owner sessions are refused | `auth.integration.test.ts` | integration | implemented — including a disabled account failing closed |

## What "gate-only" means, and why it matters

Several rows are marked `gate-only`. The decision gate is implemented, tested and refuses
correctly; what does not exist is a producer of the evidence the gate consumes. Coverage
cannot reach COMPLETE against a real account because nothing yet proves a movement universe.
No fee policy can dispatch because no pre-trade bound has been derived.

This is the honest state of the milestone. A green run of these tests proves the gate refuses
what it should. It does not prove the product can execute anything, and the code reports
execution as unavailable for exactly this reason.

## Release closure

`tools/proof-manifest.ts` discovers the scenario identifiers directly from `TEST-PLAN.md` and
binds every artifact to its digest, the repository commit, lockfile, test plan, migration set,
stable account digest, epoch and adapter version. `pnpm release:gate -- --bundle <directory>`
refuses a dirty or stale bundle, a missing scenario, a wrong proof class, a local fixture labeled
as venue proof, or an initial-production release that has not passed the testnet boundary.

The gate is intentionally red at this revision. No credentialed testnet evidence bundle exists,
so T-026, T-045 and T-053 have no acceptable venue proof. Browser evidence for T-045 through
T-050, a response-loss process artifact, the T-052 mutation report and a complete final-head
regression manifest are also absent. `docs/release-readiness.md` is the current blocker ledger;
it is not a release signoff.
